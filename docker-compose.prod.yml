services:
  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile.prod
    env_file:
      - ./.env
    environment:
      - NODE_ENV=production
      # Предпочитать IPv4 для DNS‑резолвинга Node.js (снижает риск обрывов/таймаутов)
      - NODE_OPTIONS=--dns-result-order=ipv4first
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      - bot

  bot:
    build:
      context: ./apps/bot
    env_file:
      - ./.env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      - web
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DOMAIN=${DOMAIN}
      - ACME_EMAIL=${ACME_EMAIL:-}
    volumes:
      - ./infra/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  caddy_data:
  caddy_config:
